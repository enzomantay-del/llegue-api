name: llegue_backend
description: API de Llegué — círculos familiares y eventos entre dispositivos.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
